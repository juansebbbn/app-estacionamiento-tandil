spring.application.name=app_estacionamiento_tandil

# ===============================
# DATASOURCE (MySQL Docker)
# ===============================
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:estacionamiento_tandil}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:estacionamiento_user}
spring.datasource.password=${DB_PASSWORD:estacionamiento_pass}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# LOGS
# ===============================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.web=DEBUG

# Solo muestra logs tuyos y errores graves del sistema
logging.level.root=WARN

# Permitir que tus clases SÍ muestren INFO (cambiá 'com.tuapp' por tu paquete real)
logging.level.com.juan.app_estacionamiento_tandil=INFO

# Opcional: Apagar el dibujo del logo de Spring al arrancar
spring.main.banner-mode=off



